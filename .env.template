# ========================================
# MOBASHER STAGING-LIVE CONFIGURATION
# ========================================
# This file contains dummy credentials for DigitalOcean managed services
# Replace these values with your actual managed database credentials

# PostgreSQL (DigitalOcean Managed Database)
# Replace with your actual PostgreSQL managed database credentials
DB_HOST=your-postgres-host.db.ondigitalocean.com
DB_PORT=25060
DB_NAME=mobasher
DB_USER=your-postgres-username
DB_PASSWORD=your-postgres-password
DB_SSLMODE=require

# Redis (DigitalOcean Managed Redis)
# Replace with your actual Redis managed database credentials
# Note: Use 'rediss://' (with double 's') for SSL-enabled connections
# The /0 specifies database 0 - use /1, /2, etc. for other projects on same instance
REDIS_URL=rediss://your-redis-username:your-redis-password@your-redis-host.db.ondigitalocean.com:25061/0

# API Configuration
API_HOST=0.0.0.0
API_PORT=8010

# Data Root Path (using the volume we mounted)
MOBASHER_DATA_ROOT=/root/MediaView/mobasher/data

# ASR (Automatic Speech Recognition) Settings
ASR_MODEL=large-v3
ASR_DEVICE=cpu
ASR_BEAM=5
ASR_VAD=1
ASR_WORD_TS=1
ASR_COND_PREV=0
ASR_INITIAL_PROMPT=

# Metrics Ports for Monitoring
ASR_METRICS_PORT=9109
NLP_METRICS_PORT=9112

# NLP Dictionary Paths
ALERTS_DICTIONARIES_DIR=data/dictionaries/alerts
ENTITIES_DICTIONARIES_DIR=data/dictionaries/entities

# ========================================
# DEPLOYMENT NOTES:
# ========================================
# 1. Replace all 'your-*' placeholders with actual DigitalOcean credentials
# 2. Get connection details from DigitalOcean Control Panel > Databases
# 3. Ensure your droplet/server is added to the database's trusted sources
# 4. Test connections before deploying workers
